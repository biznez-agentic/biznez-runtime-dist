apiVersion: v1
kind: Service
metadata:
  name: {{ include "biznez.fullname" . }}-backend
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "biznez.componentLabels" (dict "root" . "component" "backend") | nindent 4 }}
  {{- with .Values.backend.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.backend.service.type | default "ClusterIP" }}
  ports:
    - name: http
      port: {{ .Values.backend.service.port | default 8000 }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "biznez.componentSelectorLabels" (dict "root" . "component" "backend") | nindent 4 }}
