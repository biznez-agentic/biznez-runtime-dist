{{- if .Values.gateway.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "biznez.fullname" . }}-gateway
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "biznez.componentLabels" (dict "root" . "component" "gateway") | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.gateway.config | nindent 4 }}
{{- end }}
