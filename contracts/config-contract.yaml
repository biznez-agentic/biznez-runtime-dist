# Pinned copy of config-contract.yaml from the runtime repo
# Updated at release time by release/build-release.sh
# Source of truth: biznez-agentic-runtime/config-contract.yaml
#
# Placeholder -- populated at first release
